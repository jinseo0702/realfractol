#include "../include/fractol.h"

int main(void)
{
	t_data data;
	// t_complex *val = (t_complex *)malloc(sizeof(t_complex));

	// if (argc == 2)
	// {
		// init_complex(val);
		init_mlx(&data);
		exit(0);
	// }
	// printf("Error\n");
	return (0);
}